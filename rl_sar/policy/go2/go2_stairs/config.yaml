# Copyright (c) 2024-2025 Ziqi Fan
# SPDX-License-Identifier: Apache-2.0

go2/go2_stairs:
  model_name: "policy_1.pt"
  num_observations: 45
  observations: ["commands", "ang_vel", "base_rpy", "dof_pos", "dof_vel", "actions"]
  observations_history: [0]
  observations_history_priority: "time"
  clip_obs: 100.0
  clip_actions_lower: [-100, -100, -100,  # 动作裁剪下界
                       -100, -100, -100,
                       -100, -100, -100,
                       -100, -100, -100]
  clip_actions_upper: [100, 100, 100,  # 动作裁剪上界
                       100, 100, 100,
                       100, 100, 100,
                       100, 100, 100]
  rl_kp: [20.0, 20.0, 20.0,  # RL 控制 P 增益
          20.0, 20.0, 20.0,
          20.0, 20.0, 20.0,
          20.0, 20.0, 20.0]
  rl_kd: [0.5, 0.5, 0.5,  # RL 控制 D 增益
          0.5, 0.5, 0.5,
          0.5, 0.5, 0.5,
          0.5, 0.5, 0.5]
  fixed_kp: [60, 60, 60,  # 固定姿态控制 P 增益
             60, 60, 60,
             60, 60, 60,
             60, 60, 60]
  fixed_kd: [5, 5, 5,  # 固定姿态控制 D 增益
             5, 5, 5,
             5, 5, 5,
             5, 5, 5]
  num_of_dofs: 12
  action_scale: [0.25, 0.25, 0.25,  # 动作缩放
                 0.25, 0.25, 0.25,
                 0.25, 0.25, 0.25,
                 0.25, 0.25, 0.25]
  wheel_indices: []
  lin_vel_scale: 2.0  # 线速度缩放（未在观测中使用 but 保持一致）
  ang_vel_scale: 0.25  # 角速度观测缩放
  dof_pos_scale: 1.0  # 关节位置缩放
  dof_vel_scale: 0.05  # 关节速度缩放
  commands_scale: [1.0, 1.0, 1.0]  # 指令缩放（保持原始值）
  torque_limits: [23.7, 23.7, 35.55,  # 力矩限制（来自 URDF）
                  23.7, 23.7, 35.55,
                  23.7, 23.7, 35.55,
                  23.7, 23.7, 35.55]
  default_dof_pos: [ 0.1000, 0.8000, -1.5000,  # 默认关节姿态（FL）
                    -0.1000, 0.8000, -1.5000,  # FR
                     0.1000, 1.0000, -1.5000,  # RL
                    -0.1000, 1.0000, -1.5000]  # RR
  joint_mapping: [3, 4, 5, 0, 1, 2, 9, 10, 11, 6, 7, 8]  # 策略关节→电机关节映射
